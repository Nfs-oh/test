import styled from 'styled-components';

export const Container = styled.div`
  .collapse-wrap {
    display: flex;
  }
  .collapse-ellipse {
    text-overflow: ellipsis;
    white-space: nowrap;
    flex: 1;
    overflow: hidden;
  }
  .collapse-text-btn {
    flex-shink: 0;
  }
`;
